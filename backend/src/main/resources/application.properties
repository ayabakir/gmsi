# ========== H2 DATABASE ==========
spring.datasource.url=jdbc:h2:file:./data/gmsi_db
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# ========== H2 CONSOLE ==========
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ========== JPA / HIBERNATE ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# ========== SERVEUR ==========
server.port=8080

# ========== JWT ==========
gmsi.jwt.secret=gmsi_secret_key_2026_very_long_secure_key_minimum_256_bits!!
gmsi.jwt.expiration-ms=86400000


logging.level.org.springframework.security=DEBUG

# ??? Configuration SMTP (Gmail) ???????????????????????????????????????????????
# Remplacer les valeurs par les variables d'environnement ou vos credentials
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:ikramrobaii136@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Yacwd2055}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ??? Configuration VAPID (Push Web) ???????????????????????????????????????????
# Générer les clés VAPID avec : npx web-push generate-vapid-keys
# Puis les exporter en variables d'environnement VAPID_PUBLIC / VAPID_PRIVATE
gmsi.vapid.public-key=${VAPID_PUBLIC:}
gmsi.vapid.private-key=${VAPID_PRIVATE:}
gmsi.vapid.subject=mailto:admin@gmsi.ma